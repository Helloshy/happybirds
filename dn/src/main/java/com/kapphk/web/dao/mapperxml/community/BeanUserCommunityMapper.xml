<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kapphk.web.dao.mapper.community.BeanUserCommunityMapper" >
    <resultMap id="BaseResultMap" type="com.kapphk.web.bean.community.BeanUserCommunity" >
        <!--  自动生成 2016年10月31日 表与实体类字段对应 -->
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="uid" property="uid" jdbcType="BIGINT" />
        <result column="community_id" property="communityId" jdbcType="BIGINT" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="record_type" property="recordType" jdbcType="INTEGER" />
    </resultMap>
    
    <!-- 获取社区成员 -->
    <select id="findByCommunityId" resultType="java.lang.String">
    	SELECT CONCAT('alias',CAST(uid AS CHAR)) FROM user_community WHERE community_id IN 
    	<foreach collection="cid" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </select>
    
    <!-- 删除一级客户跟从的社区信息 -->
    <delete id="deleteByuids">
    	DELETE FROM user_community
    	<where>
    		 record_type=2 AND uid=#{uid}
    		 <if test="ids != null">
    		 	OR uid IN 
    		 	<foreach collection="ids" item="item" open="(" separator="," close=")" >
		            #{item}
		        </foreach>
    		 </if>
    	</where>
    </delete>
    
    <!-- 社区公告、动态  zoenyu 16-11-2 -->
    <select id="searchNoticeOrDynamic" resultType="java.util.Map">
    	<choose>
    		<when test="uid != null">
	    		SELECT acc.id,acc.uid,acc.content,su.logo_path as sendLogoPath,su.nick_name as nickName,
				CONCAT_WS(',',acc.logo_path,acc.logo_path2,acc.logo_path3) AS logoPath,
				acc.likes,CAST(acc.create_time AS CHAR) AS createTime,ss.value AS iconValue,
				COUNT(DISTINCT accn.id) AS isPoint,
				COUNT(DISTINCT acct.id) AS commentCounts 
				FROM app_community_content AS acc 
				INNER JOIN sys_user AS su ON acc.uid = su.id 
				LEFT JOIN app_community_collection AS accn ON acc.id = accn.content_id AND accn.uid = #{uid}
				LEFT JOIN app_content_comment AS acct ON acc.id = acct.content_id 
				LEFT JOIN sys_setting AS ss ON acc.icon_value = ss.id 
				INNER JOIN user_community AS uc ON acc.community_id = uc.community_id  
				where acc.record_type = #{type} AND acc.community_id = #{id} 
				GROUP BY acc.id order by acc.id desc limit #{page},#{rows}
    		</when>
    		<otherwise>
    			SELECT acc.id,acc.uid,acc.content,su.logo_path as sendLogoPath,su.nick_name as nickName,
				CONCAT_WS(',',acc.logo_path,acc.logo_path2,acc.logo_path3) AS logoPath,
				acc.likes,CAST(acc.create_time AS CHAR) AS createTime,ss.value AS iconValue,
				0 AS isPoint,
				COUNT(DISTINCT acct.id) AS commentCounts 
				FROM app_community_content AS acc 
				INNER JOIN sys_user AS su ON acc.uid = su.id 
				LEFT JOIN app_content_comment AS acct ON acc.id = acct.content_id 
				LEFT JOIN sys_setting AS ss ON acc.icon_value = ss.id 
				INNER JOIN user_community AS uc ON acc.community_id = uc.community_id  
				where acc.record_type = #{type} AND acc.community_id = #{id} 
				GROUP BY acc.id order by acc.id desc limit #{page},#{rows}
    		</otherwise>
    	</choose>
    	
    </select>
    
    <!-- 查询列表总数 -->
    <select id="findCount" resultType="java.lang.Integer">
    	SELECT COUNT(1)
		FROM user_community uc
		INNER JOIN app_community c ON c.id=uc.community_id
		INNER JOIN sys_user su ON su.id=c.uid
		INNER JOIN sys_user u ON u.id=uc.uid
		<where>
			<if test="itemName != null and itemName != ''">
				and c.item_name like '%${itemName}%'
			</if>
			<if test="userName != null and userName != ''">
				and su.user_name like '%${userName}%'
			</if>
			<if test="realName != null and realName != ''">
				and u.real_name like '%${realName}%'
			</if>
		</where>
    </select>
    
    <!-- 查询列表 -->
    <select id="findList" resultType="java.util.Map">
    	SELECT uc.id,c.item_name AS itemName,su.user_name AS managerPhone,su.real_name AS managerName,
		'社区成员'AS tagValue,u.logo_path AS logoPath,u.real_name AS realName,u.user_name AS userName,
		u1.user_name AS sjUserName,u1.real_name AS sjRealName,CAST(uc.create_time AS CHAR) AS createTime
		FROM user_community uc
		INNER JOIN app_community c ON c.id=uc.community_id
		INNER JOIN sys_user su ON su.id=c.uid
		INNER JOIN sys_user u ON u.id=uc.uid
		LEFT JOIN sys_user u1 ON u1.id=u.uid_from
		<where>
			<if test="itemName != null and itemName != ''">
				and c.item_name like '%${itemName}%'
			</if>
			<if test="userName != null and userName != ''">
				and su.user_name like '%${userName}%'
			</if>
			<if test="realName != null and realName != ''">
				and u.real_name like '%${realName}%'
			</if>
		</where>
		ORDER BY uc.create_time DESC LIMIT #{page},#{rows}
    </select>
    
    <sql id="Base_Column_List" >
        <!--  自动生成 2016年10月31日 表字段名称 -->
        id, uid, community_id, create_time,record_type
    </sql>
    
    <sql id="findByPageSql" >
        <!-- WARNING -  自动生成 2016-10-31  分页查询SQL -->
        select a.* from user_community a
        <where >
            <if test="param.id != null " >
                and a.id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.uid != null " >
                and a.uid = #{param.uid,jdbcType=BIGINT}
            </if>
            <if test="param.communityId != null " >
                and a.community_id = #{param.communityId,jdbcType=BIGINT}
            </if>
             <if test="param.recordType != null " >
                and a.record_type = #{param.recordType,jdbcType=INTEGER}
            </if>
        </where>
        order by a.id desc
    </sql>
    
    <select id="findByPage" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-31  分页查询 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp limit #{offset}, #{pageSize}
    </select>
    
    <select id="findByPageCount" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-10-31  分页查询总条数 -->
        select count(1) from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Long" >
        <!--  自动生成 2016年10月31日 根据ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from user_community
        where id = #{id,jdbcType=BIGINT}
    </select>
    
    <select id="load" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-31  根据多个ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from user_community where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </select>
    
    <select id="all" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-31  查询所有 -->
        select 
        <include refid="Base_Column_List" />
        from user_community
    </select>
    
    <select id="count" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-10-31 查询总条数 -->
        select count(1) from user_community
    </select>
    
    <update id="update" parameterType="com.kapphk.web.bean.community.BeanUserCommunity" >
        <!--  自动生成 2016年10月31日 更新数据 -->
        update user_community
        <set >
            <if test="uid != null" >
                uid = #{uid,jdbcType=BIGINT},
            </if>
            <if test="communityId != null" >
                community_id = #{communityId,jdbcType=BIGINT},
            </if>
            <if test="createTime != null" >
                create_time = #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="param.recordType != null " >
                record_type = #{param.recordType,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
    
    <insert id="insert" parameterType="com.kapphk.web.bean.community.BeanUserCommunity" useGeneratedKeys="true" keyProperty="id" >
        <!--  自动生成 2016年10月31日 添加数据 -->
        insert into user_community
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="uid != null" >
                uid,
            </if>
            <if test="communityId != null" >
                community_id,
            </if>
            <if test="createTime != null" >
                create_time,
            </if>
            <if test="recordType != null " >
                record_type,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="uid != null" >
                #{uid,jdbcType=BIGINT},
            </if>
            <if test="communityId != null" >
                #{communityId,jdbcType=BIGINT},
            </if>
            <if test="createTime != null" >
                #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="recordType != null " >
                #{recordType,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    
    <insert id="inserts" parameterType="java.util.List" >
        <!-- WARNING -  自动生成 2016-10-31 添加多条数据 -->
        insert into user_community (uid, community_id, create_time,record_type)
        values
        <foreach collection="list" item="item" separator="," >
            (#{item.uid,jdbcType=BIGINT}, #{item.communityId,jdbcType=BIGINT}, #{item.createTime,jdbcType=TIMESTAMP}, #{item.recordType,jdbcType=INTEGER})
        </foreach>
    </insert>
    
    <delete id="delete" parameterType="java.lang.Long" >
        <!--  自动生成 2016年10月31日 删除数据 -->
        delete from user_community
        where id = #{id,jdbcType=BIGINT}
    </delete>
    
    <delete id="deletes" >
        <!-- WARNING -  自动生成 2016-10-31 删除多条数据 -->
        delete from user_community where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </delete>
    
    <delete id="deleteByEntity" >
        <!-- WARNING -  自动生成 2016-10-31 通过实体类中的条件删除数据 -->
        delete from user_community
        <where >
            <if test="param.id != null " >
                and id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.uid != null " >
                and uid = #{param.uid,jdbcType=BIGINT}
            </if>
            <if test="param.communityId != null " >
                and community_id = #{param.communityId,jdbcType=BIGINT}
            </if>
             <if test="param.recordType != null " >
                and record_type = #{param.recordType,jdbcType=INTEGER}
            </if>
        </where>
    </delete>
    
    <select id="findAll" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-31  通过实体中的条件查询所有 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
</mapper>