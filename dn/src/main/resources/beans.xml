<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
						http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
						http://www.springframework.org/schema/context
						http://www.springframework.org/schema/context/spring-context-3.0.xsd
						">
	<!-- 引入属性文件 -->
	<context:property-placeholder location="classpath:jdbc.properties" />
	<!-- 自动扫描(自动注入) -->
	<context:component-scan base-package="com.kapphk.web,com.alipay.*.service" />
	
	<!-- 目标定时任务类 -->
	<bean id="myQuartz" class="com.kapphk.web.task.ManagerBonus"></bean> 
	
    <!-- 调度工厂Bean -->
	<bean id="jobDetailBean" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject" ref="myQuartz" />
	    <property name="targetMethod" value="managerBonus" />
	    <property name="concurrent" value="false" />
	</bean>
	
	<!-- 配置调度任务执行的触发的时间 -->
	<bean id="myTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">  
		<property name="jobDetail" ref="jobDetailBean" />
		   <property name="cronExpression">
		     <value>0 0 0 1 * ?</value>
		</property>
	</bean>
	
	<!-- 调度工厂Bean -->
	<bean id="jobDetailBean1" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject" ref="myQuartz" />
	    <property name="targetMethod" value="teacherOrder" />
	    <property name="concurrent" value="false" />
	</bean>
	
	<!-- 配置调度任务执行的触发的时间 -->
	<bean id="myTrigger1" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">  
		<property name="jobDetail" ref="jobDetailBean1" />
		   <property name="cronExpression">
		     <value>0 0 0 * * ?</value>
		</property>
	</bean>
	
	<!-- 调度工厂Bean -->
	<bean id="jobDetailBean2" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject" ref="myQuartz" />
	    <property name="targetMethod" value="cancellationOfOrder" />
	    <property name="concurrent" value="false" />
	</bean>
	
	<!-- 配置调度任务执行的触发的时间 -->
	<bean id="myTrigger2" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">  
		<property name="jobDetail" ref="jobDetailBean2" />
		   <property name="cronExpression">
		     <value>0 0/5 * * * ?</value>
		</property>
	</bean>
	
    <!-- D.Quartz的调度工厂，调度工厂只能有一个，多个调度任务在list中添加 -->
    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">  
      <property name="triggers">
      	<list>
      		<ref bean="myTrigger"/>
      		<ref bean="myTrigger1"/>
      		<ref bean="myTrigger2"/>
      	</list>  
      </property>
    </bean>
	
</beans>