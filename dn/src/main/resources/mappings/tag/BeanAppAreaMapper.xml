<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kapphk.web.dao.mapper.tag.BeanAppAreaMapper" >
    <resultMap id="BaseResultMap" type="com.kapphk.web.bean.tag.BeanAppArea" >
        <!--  自动生成 2016年10月28日 表与实体类字段对应 -->
        <id column="id" property="id" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    </resultMap>
    
    <!-- 获取全部区域 -->
    <select id="findAreaList" resultType="java.util.Map">
    	SELECT id, id as `text` FROM app_area
    </select>
    
    <!-- 详情 -->
    <select id="findDetailById" resultType="java.util.Map">
    	SELECT a.id,a.id as oldId,(SELECT GROUP_CONCAT(d.pid) FROM app_area_detail d WHERE d.area_id=a.id) AS areaDetail 
		FROM app_area a where id=#{id}
    </select>
    
    <!-- 查询列表 -->
    <select id="findList" resultType="java.util.Map">
    	SELECT a.id,CAST(a.create_time AS CHAR) AS createTime,(SELECT GROUP_CONCAT(d.pid) FROM app_area_detail d WHERE d.area_id=a.id) AS areaDetail 
		FROM app_area a ORDER BY a.create_time DESC LIMIT #{page},#{rows}
    </select>
    
    <sql id="Base_Column_List" >
        <!--  自动生成 2016年10月28日 表字段名称 -->
        id, create_time
    </sql>
    
    <sql id="findByPageSql" >
        <!-- WARNING -  自动生成 2016-10-28  分页查询SQL -->
        select a.* from app_area a
        <where >
            <if test="param.id != null and param.id != ''" >
                and a.id = #{param.id,jdbcType=VARCHAR}
            </if>
        </where>
        order by a.id desc
    </sql>
    
    <select id="findByPage" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-28  分页查询 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp limit #{offset}, #{pageSize}
    </select>
    
    <select id="findByPageCount" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-10-28  分页查询总条数 -->
        select count(1) from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.String" >
        <!--  自动生成 2016年10月28日 根据ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from app_area
        where id = #{id,jdbcType=VARCHAR}
    </select>
    
    <select id="load" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-28  根据多个ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from app_area where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </select>
    
    <select id="all" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-28  查询所有 -->
        select 
        <include refid="Base_Column_List" />
        from app_area
    </select>
    
    <select id="count" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-10-28 查询总条数 -->
        select count(1) from app_area
    </select>
    
    <update id="update" parameterType="com.kapphk.web.bean.tag.BeanAppArea" >
        <!--  自动生成 2016年10月28日 更新数据 -->
        update app_area
        <set >
        	<if test="id != null" >
                id = #{id,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null" >
                create_time = #{createTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        where id = #{oldId,jdbcType=VARCHAR}
    </update>
    
    <insert id="insert" parameterType="com.kapphk.web.bean.tag.BeanAppArea" useGeneratedKeys="true" keyProperty="id" >
        <!--  自动生成 2016年10月28日 添加数据 -->
        insert into app_area
        <trim prefix="(" suffix=")" suffixOverrides="," >
        	<if test="id != null" >
                id,
            </if>
            <if test="createTime != null" >
                create_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
        	<if test="id != null" >
                #{id,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null" >
                #{createTime,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>
    
    <insert id="inserts" parameterType="java.util.List" >
        <!-- WARNING -  自动生成 2016-10-28 添加多条数据 -->
        insert into app_area (create_time)
        values
        <foreach collection="list" item="item" separator="," >
            (#{item.createTime,jdbcType=TIMESTAMP})
        </foreach>
    </insert>
    
    <delete id="delete" parameterType="java.lang.String" >
        <!--  自动生成 2016年10月28日 删除数据 -->
        delete from app_area
        where id = #{id,jdbcType=VARCHAR}
    </delete>
    
    <delete id="deletes" >
        <!-- WARNING -  自动生成 2016-10-28 删除多条数据 -->
        delete from app_area where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </delete>
    
    <delete id="deleteByEntity" >
        <!-- WARNING -  自动生成 2016-10-28 通过实体类中的条件删除数据 -->
        delete from app_area
        <where >
            <if test="param.id != null and param.id != ''" >
                and id = #{param.id,jdbcType=VARCHAR}
            </if>
        </where>
    </delete>
    
    <select id="findAll" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-28  通过实体中的条件查询所有 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
</mapper>