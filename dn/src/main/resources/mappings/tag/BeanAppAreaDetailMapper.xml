<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kapphk.web.dao.mapper.tag.BeanAppAreaDetailMapper" >
    <resultMap id="BaseResultMap" type="com.kapphk.web.bean.tag.BeanAppAreaDetail" >
        <!--  自动生成 2016年10月28日 表与实体类字段对应 -->
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="area_id" property="areaId" jdbcType="VARCHAR" />
        <result column="pid" property="pid" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    </resultMap>
    
    <!-- 获取全部省份 -->
    <select id="findProvinceList" resultType="java.lang.String">
    	<if test="aid_checkbox != null">
    		SELECT DISTINCT pid FROM app_area_detail
    	</if>
    	<if test="aid_checkbox == null and aid != null">
    		SELECT DISTINCT pid FROM app_area_detail WHERE area_id IN 
    		<foreach collection="aid" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    	</if>
    </select>
    
    <sql id="Base_Column_List" >
        <!--  自动生成 2016年10月28日 表字段名称 -->
        id, area_id, pid, create_time
    </sql>
    
    <sql id="findByPageSql" >
        <!-- WARNING -  自动生成 2016-10-28  分页查询SQL -->
        select a.* from app_area_detail a
        <where >
            <if test="param.id != null " >
                and a.id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.areaId != null and param.areaId != ''" >
                and a.area_id = #{param.areaId,jdbcType=VARCHAR}
            </if>
            <if test="param.pid != null and param.pid != ''" >
                and a.pid = #{param.pid,jdbcType=VARCHAR}
            </if>
        </where>
        order by a.id desc
    </sql>
    
    <select id="findByPage" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-28  分页查询 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp limit #{offset}, #{pageSize}
    </select>
    
    <select id="findByPageCount" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-10-28  分页查询总条数 -->
        select count(1) from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Long" >
        <!--  自动生成 2016年10月28日 根据ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from app_area_detail
        where id = #{id,jdbcType=BIGINT}
    </select>
    
    <select id="load" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-28  根据多个ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from app_area_detail where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </select>
    
    <select id="all" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-28  查询所有 -->
        select 
        <include refid="Base_Column_List" />
        from app_area_detail
    </select>
    
    <select id="count" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-10-28 查询总条数 -->
        select count(1) from app_area_detail
    </select>
    
    <update id="update" parameterType="com.kapphk.web.bean.tag.BeanAppAreaDetail" >
        <!--  自动生成 2016年10月28日 更新数据 -->
        update app_area_detail
        <set >
            <if test="areaId != null" >
                area_id = #{areaId,jdbcType=VARCHAR},
            </if>
            <if test="pid != null" >
                pid = #{pid,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null" >
                create_time = #{createTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
    
    <insert id="insert" parameterType="com.kapphk.web.bean.tag.BeanAppAreaDetail" useGeneratedKeys="true" keyProperty="id" >
        <!--  自动生成 2016年10月28日 添加数据 -->
        insert into app_area_detail
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="areaId != null" >
                area_id,
            </if>
            <if test="pid != null" >
                pid,
            </if>
            <if test="createTime != null" >
                create_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="areaId != null" >
                #{areaId,jdbcType=VARCHAR},
            </if>
            <if test="pid != null" >
                #{pid,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null" >
                #{createTime,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>
    
    <insert id="inserts" parameterType="java.util.List" >
        <!-- WARNING -  自动生成 2016-10-28 添加多条数据 -->
        insert into app_area_detail (area_id, pid, create_time)
        values
        <foreach collection="list" item="item" separator="," >
            (#{item.areaId,jdbcType=VARCHAR}, #{item.pid,jdbcType=VARCHAR}, #{item.createTime,jdbcType=TIMESTAMP})
        </foreach>
    </insert>
    
    <delete id="delete" parameterType="java.lang.Long" >
        <!--  自动生成 2016年10月28日 删除数据 -->
        delete from app_area_detail
        where id = #{id,jdbcType=BIGINT}
    </delete>
    
    <delete id="deletes" >
        <!-- WARNING -  自动生成 2016-10-28 删除多条数据 -->
        delete from app_area_detail where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </delete>
    
    <delete id="deleteByEntity" >
        <!-- WARNING -  自动生成 2016-10-28 通过实体类中的条件删除数据 -->
        delete from app_area_detail
        <where >
            <if test="param.id != null " >
                and id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.areaId != null and param.areaId != ''" >
                and area_id = #{param.areaId,jdbcType=VARCHAR}
            </if>
            <if test="param.pid != null and param.pid != ''" >
                and pid = #{param.pid,jdbcType=VARCHAR}
            </if>
        </where>
    </delete>
    
    <select id="findAll" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-28  通过实体中的条件查询所有 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
</mapper>