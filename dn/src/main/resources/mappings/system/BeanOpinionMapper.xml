<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kapphk.web.dao.mapper.system.BeanOpinionMapper" >
    <resultMap id="BaseResultMap" type="com.kapphk.web.bean.system.BeanOpinion" >
        <!--  自动生成 2016年09月20日 表与实体类字段对应 -->
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="uid" property="uid" jdbcType="BIGINT" />
        <result column="content" property="content" jdbcType="VARCHAR" />
        <result column="state" property="state" jdbcType="INTEGER" />
        <result column="feedback" property="feedback" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="record_type" property="recordType" jdbcType="INTEGER" />
        <result column="feedback_time" property="feedbackTime" jdbcType="TIMESTAMP" />
    </resultMap>
    
    <!-- 查询列表 -->
    <select id="findList" resultType="java.util.Map">
    	SELECT u.user_name AS userName,u.nick_name AS nickName,o.id,o.content,CAST(o.create_time AS CHAR) AS createTime,o.feedback,
    	o.state,o.state AS state1,o.record_type as recordType
		FROM user_opinion o INNER JOIN sys_user u ON o.uid=u.id
		<where>
			o.record_type = #{param.recordType}
			<if test="param.state != null">
				AND o.state = #{param.state}
			</if>
		</where>
		ORDER BY o.create_time DESC limit #{page},#{rows}
    </select>
    
    <!-- 查询列表总数 -->
    <select id="findCount" resultType="java.lang.Integer">
    	SELECT COUNT(1) FROM user_opinion
    	<where>
			record_type = #{param.recordType}
			<if test="param.state != null">
				AND state = #{param.state}
			</if>
		</where>
    </select>
    
    <!-- 详情 -->
    <select id="findDetailById" resultType="java.util.Map">
    	SELECT u.user_name AS userName,u.nick_name AS nickName,o.id,o.content,CAST(o.create_time AS CHAR) AS createTime,o.feedback,
    	o.state,o.state AS state1,o.record_type as recordType
		FROM user_opinion o INNER JOIN sys_user u ON o.uid=u.id where o.id = #{id}
    </select>
    
    <sql id="Base_Column_List" >
        <!--  自动生成 2016年09月20日 表字段名称 -->
        id, uid, content, state, feedback, create_time,record_type,feedback_time
    </sql>
    
    <sql id="findByPageSql" >
        <!-- WARNING -  自动生成 2016-09-20  分页查询SQL -->
        select a.* from user_opinion a
        <where >
            <if test="param.id != null " >
                and a.id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.uid != null " >
                and a.uid = #{param.uid,jdbcType=BIGINT}
            </if>
            <if test="param.content != null and param.content != ''" >
                and a.content = #{param.content,jdbcType=VARCHAR}
            </if>
            <if test="param.state != null " >
                and a.state = #{param.state,jdbcType=INTEGER}
            </if>
            <if test="param.feedback != null and param.feedback != ''" >
                and a.feedback = #{param.feedback,jdbcType=VARCHAR}
            </if>
            <if test="param.recordType != null and param.recordType != ''" >
                and a.record_type = #{param.recordType,jdbcType=INTEGER}
            </if>
        </where>
        order by a.id desc
    </sql>
    
    <select id="findByPage" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-09-20  分页查询 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp limit #{offset}, #{pageSize}
    </select>
    
    <select id="findByPageCount" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-09-20  分页查询总条数 -->
        select count(1) from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Long" >
        <!--  自动生成 2016年09月20日 根据ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from user_opinion
        where id = #{id,jdbcType=BIGINT}
    </select>
    
    <select id="load" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-09-20  根据多个ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from user_opinion where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </select>
    
    <select id="all" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-09-20  查询所有 -->
        select 
        <include refid="Base_Column_List" />
        from user_opinion
    </select>
    
    <select id="count" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-09-20 查询总条数 -->
        select count(1) from user_opinion
    </select>
    
    <update id="update" parameterType="com.kapphk.web.bean.system.BeanOpinion" >
        <!--  自动生成 2016年09月20日 更新数据 -->
        update user_opinion
        <set >
            <if test="uid != null" >
                uid = #{uid,jdbcType=BIGINT},
            </if>
            <if test="content != null" >
                content = #{content,jdbcType=VARCHAR},
            </if>
            <if test="state != null" >
                state = #{state,jdbcType=INTEGER},
            </if>
            <if test="feedback != null" >
                feedback = #{feedback,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null" >
                create_time = #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="recordType != null" >
                record_type = #{recordType,jdbcType=INTEGER},
            </if>
            <if test="feedbackTime != null" >
                feedback_time = #{feedbackTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
    
    <insert id="insert" parameterType="com.kapphk.web.bean.system.BeanOpinion" useGeneratedKeys="true" keyProperty="id" >
        <!--  自动生成 2016年09月20日 添加数据 -->
        insert into user_opinion
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="uid != null" >
                uid,
            </if>
            <if test="content != null" >
                content,
            </if>
            <if test="state != null" >
                state,
            </if>
            <if test="feedback != null" >
                feedback,
            </if>
            <if test="createTime != null" >
                create_time,
            </if>
            <if test="recordType != null">
            	record_type,
            </if>
            <if test="feedbackTime != null" >
                feedback_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="uid != null" >
                #{uid,jdbcType=BIGINT},
            </if>
            <if test="content != null" >
                #{content,jdbcType=VARCHAR},
            </if>
            <if test="state != null" >
                #{state,jdbcType=INTEGER},
            </if>
            <if test="feedback != null" >
                #{feedback,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null" >
                #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="recordType != null" >
                #{recordType,jdbcType=INTEGER},
            </if>
            <if test="feedbackTime != null" >
                #{feedbackTime,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>
    
    <insert id="inserts" parameterType="java.util.List" >
        <!-- WARNING -  自动生成 2016-09-20 添加多条数据 -->
        insert into user_opinion (uid, content, state, feedback, create_time,record_type,feedback_time)
        values
        <foreach collection="list" item="item" separator="," >
            (#{item.uid,jdbcType=BIGINT}, #{item.content,jdbcType=VARCHAR}, #{item.state,jdbcType=INTEGER}, 
            #{item.feedback,jdbcType=VARCHAR}, #{item.createTime,jdbcType=TIMESTAMP},#{item.recordType,jdbcType=INTEGER}, #{item.feedbackTime,jdbcType=TIMESTAMP})
        </foreach>
    </insert>
    
    <delete id="delete" parameterType="java.lang.Long" >
        <!--  自动生成 2016年09月20日 删除数据 -->
        delete from user_opinion
        where id = #{id,jdbcType=BIGINT}
    </delete>
    
    <delete id="deletes" >
        <!-- WARNING -  自动生成 2016-09-20 删除多条数据 -->
        delete from user_opinion where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </delete>
    
    <delete id="deleteByEntity" >
        <!-- WARNING -  自动生成 2016-09-20 通过实体类中的条件删除数据 -->
        delete from user_opinion
        <where >
            <if test="param.id != null " >
                and id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.uid != null " >
                and uid = #{param.uid,jdbcType=BIGINT}
            </if>
            <if test="param.content != null and param.content != ''" >
                and content = #{param.content,jdbcType=VARCHAR}
            </if>
            <if test="param.state != null " >
                and state = #{param.state,jdbcType=INTEGER}
            </if>
            <if test="param.feedback != null and param.feedback != ''" >
                and feedback = #{param.feedback,jdbcType=VARCHAR}
            </if>
            <if test="param.recordType != null and param.recordType != ''" >
                and record_type = #{param.recordType,jdbcType=INTEGER}
            </if>
        </where>
    </delete>
    
    <select id="findAll" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-09-20  通过实体中的条件查询所有 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
</mapper>