<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kapphk.web.dao.mapper.system.BeanSystemUserMapper" >
    <resultMap id="BaseResultMap" type="com.kapphk.web.bean.system.BeanSystemUser" >
        <!--  自动生成 2016年05月23日 表与实体类字段对应 -->
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="user_name" property="userName" jdbcType="VARCHAR" />
        <result column="pwd" property="pwd" jdbcType="VARCHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="state" property="state" jdbcType="SMALLINT" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="remark" property="remark" jdbcType="VARCHAR" />
    </resultMap>
    
     <!-- 详情 -->
    <select id="findDetail" resultType="java.util.Map">
    	SELECT su.id,su.user_name AS userName,su.state,ur.role_id AS roleId
		FROM sys_system_user su 
		LEFT JOIN sys_system_user_role ur ON ur.user_id=su.id
		WHERE su.id=#{id}
    </select>
    
    <!-- 查询管理员列表 -->
    <select id="findList" resultType="java.util.Map">
    	SELECT su.id,su.user_name AS userName,su.state,CAST(su.create_time AS CHAR) as createTime,r.role_name AS roleName 
		FROM sys_system_user su 
		LEFT JOIN sys_system_user_role ur ON ur.user_id=su.id
		LEFT JOIN sys_role r ON r.id=ur.role_id
		ORDER BY su.create_time DESC LIMIT #{page},#{rows}
    </select>
    
    <sql id="Base_Column_List" >
        <!--  自动生成 2016年05月23日 表字段名称 -->
        id, user_name, pwd, name, state, create_time, remark
    </sql>
    
    <sql id="findByPageSql" >
        <!-- WARNING -  自动生成 2016-05-23  分页查询SQL -->
        select a.* from sys_system_user a
        <where >
            <if test="param.id != null " >
                and a.id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.userName != null and param.userName != ''" >
                and a.user_name = #{param.userName,jdbcType=VARCHAR}
            </if>
            <if test="param.pwd != null and param.pwd != ''" >
                and a.pwd = #{param.pwd,jdbcType=VARCHAR}
            </if>
            <if test="param.name != null and param.name != ''" >
                and a.name = #{param.name,jdbcType=VARCHAR}
            </if>
            <if test="param.state != null " >
                and a.state = #{param.state,jdbcType=SMALLINT}
            </if>
            <if test="param.remark != null and param.remark != ''" >
                and a.remark = #{param.remark,jdbcType=VARCHAR}
            </if>
        </where>
        order by a.id desc
    </sql>
    
    <select id="findByPage" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-05-23  分页查询 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp limit #{offset}, #{pageSize}
    </select>
    
    <select id="findByPageCount" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-05-23  分页查询总条数 -->
        select count(1) from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Long" >
        <!--  自动生成 2016年05月23日 根据ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from sys_system_user
        where id = #{id,jdbcType=BIGINT}
    </select>
    
    <select id="load" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-05-23  根据多个ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from sys_system_user where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </select>
    
    <select id="all" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-05-23  查询所有 -->
        select 
        <include refid="Base_Column_List" />
        from sys_system_user
    </select>
    
    <select id="count" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-05-23 查询总条数 -->
        select count(1) from sys_system_user
    </select>
    
    <update id="update" parameterType="com.kapphk.web.bean.system.BeanSystemUser" >
        <!--  自动生成 2016年05月23日 更新数据 -->
        update sys_system_user
        <set >
            <if test="userName != null" >
                user_name = #{userName,jdbcType=VARCHAR},
            </if>
            <if test="pwd != null" >
                pwd = #{pwd,jdbcType=VARCHAR},
            </if>
            <if test="name != null" >
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="state != null" >
                state = #{state,jdbcType=SMALLINT},
            </if>
            <if test="createTime != null" >
                create_time = #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="remark != null" >
                remark = #{remark,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
    
    <insert id="insert" parameterType="com.kapphk.web.bean.system.BeanSystemUser" useGeneratedKeys="true" keyProperty="id" >
        <!--  自动生成 2016年05月23日 添加数据 -->
        insert into sys_system_user
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="userName != null" >
                user_name,
            </if>
            <if test="pwd != null" >
                pwd,
            </if>
            <if test="name != null" >
                name,
            </if>
            <if test="state != null" >
                state,
            </if>
            <if test="createTime != null" >
                create_time,
            </if>
            <if test="remark != null" >
                remark,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="userName != null" >
                #{userName,jdbcType=VARCHAR},
            </if>
            <if test="pwd != null" >
                #{pwd,jdbcType=VARCHAR},
            </if>
            <if test="name != null" >
                #{name,jdbcType=VARCHAR},
            </if>
            <if test="state != null" >
                #{state,jdbcType=SMALLINT},
            </if>
            <if test="createTime != null" >
                #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="remark != null" >
                #{remark,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>
    
    <insert id="inserts" parameterType="java.util.List" >
        <!-- WARNING -  自动生成 2016-05-23 添加多条数据 -->
        insert into sys_system_user (user_name, pwd, name, state, create_time, remark)
        values
        <foreach collection="list" item="item" separator="," >
            (#{item.userName,jdbcType=VARCHAR}, #{item.pwd,jdbcType=VARCHAR}, #{item.name,jdbcType=VARCHAR}, #{item.state,jdbcType=SMALLINT}, #{item.createTime,jdbcType=TIMESTAMP}, #{item.remark,jdbcType=VARCHAR})
        </foreach>
    </insert>
    
    <delete id="delete" parameterType="java.lang.Long" >
        <!--  自动生成 2016年05月23日 删除数据 -->
        delete from sys_system_user
        where id = #{id,jdbcType=BIGINT}
    </delete>
    
    <delete id="deletes" >
        <!-- WARNING -  自动生成 2016-05-23 删除多条数据 -->
        delete from sys_system_user where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </delete>
    
    <delete id="deleteByEntity" >
        <!-- WARNING -  自动生成 2016-05-23 通过实体类中的条件删除数据 -->
        delete from sys_system_user
        <where >
            <if test="param.id != null " >
                and id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.userName != null and param.userName != ''" >
                and user_name = #{param.userName,jdbcType=VARCHAR}
            </if>
            <if test="param.pwd != null and param.pwd != ''" >
                and pwd = #{param.pwd,jdbcType=VARCHAR}
            </if>
            <if test="param.name != null and param.name != ''" >
                and name = #{param.name,jdbcType=VARCHAR}
            </if>
            <if test="param.state != null " >
                and state = #{param.state,jdbcType=SMALLINT}
            </if>
            <if test="param.remark != null and param.remark != ''" >
                and remark = #{param.remark,jdbcType=VARCHAR}
            </if>
        </where>
    </delete>
    
    <select id="findAll" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-05-23  通过实体中的条件查询所有 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
    <select id="findEntityByCondition" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-05-23  通过实体中的条件查询所有 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp limit 0,1
    </select>
    
    <select id="findByuserNameByPassword" resultMap="BaseResultMap">
    	select * from sys_system_user where user_name=#{userName} and pwd=#{password}
    </select>
</mapper>