<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kapphk.web.dao.mapper.course.BeanJoinMapper" >
    <resultMap id="BaseResultMap" type="com.kapphk.web.bean.course.BeanJoin" >
        <!--  自动生成 2016年10月22日 表与实体类字段对应 -->
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="nick_name" property="nickName" jdbcType="VARCHAR" />
        <result column="phone" property="phone" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    </resultMap>
    
    <resultMap id="ResultMapWithBLOBs" type="com.kapphk.web.bean.course.BeanJoin" extends="BaseResultMap" >
        <!--  自动生成 2016年10月22日 表与实体类字段对应 -->
        <result column="conent" property="conent" jdbcType="LONGVARCHAR" />
    </resultMap>
    
    <sql id="Base_Column_List" >
        <!--  自动生成 2016年10月22日 表字段名称 -->
        id, nick_name, phone, create_time
    </sql>
    
    <sql id="Blob_Column_List" >
        <!--  自动生成 2016年10月22日 表字段名称 -->
        conent
    </sql>
    
    <sql id="findByPageSql" >
        <!-- WARNING -  自动生成 2016-10-22  分页查询SQL -->
        select a.* from app_join a
        <where >
            <if test="param.id != null " >
                and a.id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.nickName != null and param.nickName != ''" >
                and a.nick_name = #{param.nickName,jdbcType=VARCHAR}
            </if>
            <if test="param.phone != null and param.phone != ''" >
                and a.phone = #{param.phone,jdbcType=VARCHAR}
            </if>
            <if test="param.conent != null " >
                and a.conent = #{param.conent,jdbcType=LONGVARCHAR}
            </if>
        </where>
        order by a.id desc
    </sql>
    
    <select id="findByPage" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-22  分页查询 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp limit #{offset}, #{pageSize}
    </select>
    
    <select id="findByPageCount" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-10-22  分页查询总条数 -->
        select count(1) from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
    <select id="findById" resultMap="ResultMapWithBLOBs" parameterType="java.lang.Long" >
        <!--  自动生成 2016年10月22日 根据ID查询 -->
        select 
        <include refid="Base_Column_List" />
        ,
        <include refid="Blob_Column_List" />
        from app_join
        where id = #{id,jdbcType=BIGINT}
    </select>
    
    <select id="load" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-22  根据多个ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from app_join where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </select>
    
    <select id="all" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-22  查询所有 -->
        select 
        <include refid="Base_Column_List" />
        from app_join
    </select>
    
    <select id="count" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-10-22 查询总条数 -->
        select count(1) from app_join
    </select>
    
    <update id="update" parameterType="com.kapphk.web.bean.course.BeanJoin" >
        <!--  自动生成 2016年10月22日 更新数据 -->
        update app_join
        <set >
            <if test="nickName != null" >
                nick_name = #{nickName,jdbcType=VARCHAR},
            </if>
            <if test="phone != null" >
                phone = #{phone,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null" >
                create_time = #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="conent != null" >
                conent = #{conent,jdbcType=LONGVARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
    
    <insert id="insert" parameterType="com.kapphk.web.bean.course.BeanJoin" useGeneratedKeys="true" keyProperty="id" >
        <!--  自动生成 2016年10月22日 添加数据 -->
        insert into app_join
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="nickName != null" >
                nick_name,
            </if>
            <if test="phone != null" >
                phone,
            </if>
            <if test="createTime != null" >
                create_time,
            </if>
            <if test="conent != null" >
                conent,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="nickName != null" >
                #{nickName,jdbcType=VARCHAR},
            </if>
            <if test="phone != null" >
                #{phone,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null" >
                #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="conent != null" >
                #{conent,jdbcType=LONGVARCHAR},
            </if>
        </trim>
    </insert>
    
    <delete id="delete" parameterType="java.lang.Long" >
        <!--  自动生成 2016年10月22日 删除数据 -->
        delete from app_join
        where id = #{id,jdbcType=BIGINT}
    </delete>
    
    <update id="updateByPrimaryKeyWithBLOBs" parameterType="com.kapphk.web.bean.course.BeanJoin" >
        <!--  自动生成 2016年10月22日 更新数据 -->
        update app_join
        set nick_name = #{nickName,jdbcType=VARCHAR},
            phone = #{phone,jdbcType=VARCHAR},
            create_time = #{createTime,jdbcType=TIMESTAMP},
            conent = #{conent,jdbcType=LONGVARCHAR}
        where id = #{id,jdbcType=BIGINT}
    </update>
    
    <insert id="inserts" parameterType="java.util.List" >
        <!-- WARNING -  自动生成 2016-10-22 添加多条数据 -->
        insert into app_join (nick_name, phone, create_time, conent)
        values
        <foreach collection="list" item="item" separator="," >
            (#{item.nickName,jdbcType=VARCHAR}, #{item.phone,jdbcType=VARCHAR}, #{item.createTime,jdbcType=TIMESTAMP}, #{item.conent,jdbcType=LONGVARCHAR})
        </foreach>
    </insert>
    
    <delete id="deletes" >
        <!-- WARNING -  自动生成 2016-10-22 删除多条数据 -->
        delete from app_join where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </delete>
    
    <delete id="deleteByEntity" >
        <!-- WARNING -  自动生成 2016-10-22 通过实体类中的条件删除数据 -->
        delete from app_join
        <where >
            <if test="param.id != null " >
                and id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.nickName != null and param.nickName != ''" >
                and nick_name = #{param.nickName,jdbcType=VARCHAR}
            </if>
            <if test="param.phone != null and param.phone != ''" >
                and phone = #{param.phone,jdbcType=VARCHAR}
            </if>
            <if test="param.conent != null " >
                and conent = #{param.conent,jdbcType=LONGVARCHAR}
            </if>
        </where>
    </delete>
    
    <select id="findAll" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-22  通过实体中的条件查询所有 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
    <select id="findEntityByCondition" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-10-22  通过实体中的条件查询所有 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp limit 0,1
    </select>
</mapper>